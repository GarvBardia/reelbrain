# For local development only (`npm run dev`). Nothing here is sensitive --
# NEXT_PUBLIC_ values are always inlined into the JS bundle at build time,
# which is exactly what happens in .github/workflows/deploy-pages.yml too.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
